spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/task_management_system
    username: jajangrohmatulloh
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        show_sql: true
